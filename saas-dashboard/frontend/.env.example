# In production (Vercel), set this to your deployed backend URL, e.g.:
# VITE_API_URL=https://your-backend.onrender.com/api
#
# Leave this file as-is / unset locally — the Vite dev server proxy in
# vite.config.js already forwards /api to http://localhost:4000 for you.
VITE_API_URL=
